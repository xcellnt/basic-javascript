BRANCHING:
 - we can exec program when the consdition is met

IF EXPRESSION:
 - const examValue = 80;

	if(examValue < 70) {
		console.log("get out bruh");
	}


ELSE EXPRESSION:
 - block if will exec when if condition is true
 - sometimes we need to exec another program when block if is false 
 - we can use else to exec program when block if is false
 - const examValue = 80;

	if(examValue < 70) {
		console.log("get out bruh");
	}

